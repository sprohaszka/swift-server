# Test Swift avec Docker

WIP

Swift to test with a Docker

## Reproduce

Init with `swift package init --type executable`

## Usage

```sh
  cd Sources
  swift run server
```

## Build

`swift build`